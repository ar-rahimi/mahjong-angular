import { FoundOnDescPipe } from './found-on-desc.pipe';

describe('FoundOnDescPipe', () => {
  // it('create an instance', () => {
  //   const pipe = new FoundOnDescPipe();
  //   expect(pipe).toBeTruthy();
  // });
});
